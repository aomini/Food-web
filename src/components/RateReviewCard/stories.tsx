import { Story, Meta } from '@storybook/react'
import RateReviewCard from '.'

export default {
  title: 'RateReviewCard',
  component: RateReviewCard
} as Meta

export const Default: Story = () => <RateReviewCard />
