import { Story, Meta } from '@storybook/react'
import InputField from '.'

export default {
  title: 'InputField',
  component: InputField
} as Meta

export const Default: Story = () => <InputField />
