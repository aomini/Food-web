import { Meta, Typeset } from '@storybook/addon-docs/blocks'
import Typography from '.'

<Meta title="Typography/Base" />

export const typography = {
  type: {
    primary: '"Open Sans", sans-serif'
  },
  weight: {
    regular: '400',
    semibold: '600',
    bold: '700'
  },
  size: {
    xs: 12,
    sm: 14,
    base: 16,
    lg: 18,
    xl: 20,
    '2xl': 28,
    '3xl': 32,
    '4xl': 60
  }
}

export const SampleText = 'Rakesh Shrestha is the best react developer around'

# Base Typography

**Font:** Open Sans

**Weights:** 400(regular), 500(bold), 800(extrabold), 900(black)

<Typeset
  fontSizes={[
    Number(typography.size.xs),
    Number(typography.size.sm),
    Number(typography.size.base),
    Number(typography.size.lg),
    Number(typography.size.xl),
    Number(typography.size['2xl']),
    Number(typography.size['3xl']),
    Number(typography.size['4xl'])
  ]}
  fontWeight={typography.weight.bold}
  sampleText={SampleText}
  fontFamily={typography.type.primary}
/>
