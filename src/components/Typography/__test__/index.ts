import { render } from '@testing-library/react';

import Typography from '..';

describe('<Typography />', () => {
  it('should correctly render the typography with variants', () => {});

  it('should render the typography element tag properly', () => {});
});
