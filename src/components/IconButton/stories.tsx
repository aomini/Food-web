import { Story, Meta } from '@storybook/react'
import IconButton from '.'

export default {
  title: 'IconButton',
  component: IconButton
} as Meta

export const Default: Story = () => <IconButton />
