import { Story, Meta } from '@storybook/react'
import ContactUsCard from '.'

export default {
  title: 'ContactUsCard',
  component: ContactUsCard
} as Meta

export const Default: Story = () => <ContactUsCard />
