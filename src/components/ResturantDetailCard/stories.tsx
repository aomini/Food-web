import { Story, Meta } from '@storybook/react'
import ResturantDetailCard from '.'

export default {
  title: 'ResturantDetailCard',
  component: ResturantDetailCard
} as Meta

export const Default: Story = () => <ResturantDetailCard />
