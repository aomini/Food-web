import { Story, Meta } from '@storybook/react'
import ReviewItem from '.'

export default {
  title: 'ReviewItem',
  component: ReviewItem
} as Meta

export const Default: Story = () => <ReviewItem />
