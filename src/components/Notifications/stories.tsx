import { Story, Meta } from '@storybook/react'
import Notifications from '.'

export default {
  title: 'Notifications',
  component: Notifications
} as Meta

export const Default: Story = () => <Notifications />
